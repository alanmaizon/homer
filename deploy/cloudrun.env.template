# Core server
PORT=8080

# LLM provider config (choose one provider mode)
LLM_PROVIDER=mock
LLM_TIMEOUT_MS=15000
LLM_MAX_RETRIES=2
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
GEMINI_API_KEY=
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Connector config
CONNECTOR_PROVIDER=none
CONNECTOR_API_KEY=
CONNECTOR_RATE_LIMIT_PER_MINUTE=60

# Google Docs direct credentials (optional fallback)
GOOGLE_DOCS_ACCESS_TOKEN=
GOOGLE_APPLICATION_CREDENTIALS=

# Google Docs OAuth flow (optional for connector user auth)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URL=
GOOGLE_OAUTH_SCOPES=
GOOGLE_OAUTH_STATE_TTL=10m
